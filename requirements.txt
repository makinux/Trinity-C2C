# Core dependencies (torch is installed separately from the CPU index in the Dockerfile)
numpy>=1.26
openai>=1.0
transformers>=5.0,<6
safetensors>=0.4
truststore>=0.9
pyyaml>=6.0

# API gateway + debug UI (trinity.gateway)
fastapi>=0.110
uvicorn[standard]>=0.29
